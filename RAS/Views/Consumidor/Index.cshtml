@model Consumidor

<br/>
@if(@Model == null)
{
    <h3>@ViewBag.Mensagem</h3>
}
else
{
    <h3>@ViewBag.Mensagem @Model.Nome</h3>
    <table class="table table-hover">
        <tr>
            <td>@Model.Pessoas_id</td>
            <td>@Model.Nome</td>
            <td>@Model.Cashback</td>
            <td>@Model.Faixa_etaria</td>
            <td>
                <a class="btn btn-sm btn-danger" href="/cliente/delete/@Model.Pessoas_id">Apagar</a>
                <a class="btn btn-sm btn-warning" href="/cliente/update">Editar</a>
            </td>

        </tr>
    </table>
}
<a class="btn btn-primary" href="/cliente/create">Adicionar</a>